import { Metadata } from 'next';
import { ReactWebDesignContent } from '../../../../../components/services/ReactWebDesignContent';

export const metadata: Metadata = {
    title: 'React Website Development Services | Oneskai',
    description: 'Custom React.js web applications and dynamic storefronts engineered for high-performance user experiences and interactive interfaces.',
};

export default function ReactWebDesignPage() {
    return <ReactWebDesignContent />;
}
